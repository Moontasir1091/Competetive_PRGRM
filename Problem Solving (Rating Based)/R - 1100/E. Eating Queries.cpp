#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main() {
    ll tt;
    cin >> tt;
    while(tt--) {
        ll n, q;
        cin >> n >> q;
        vector<ll> a(n);
        for(ll i = 0; i < n; i++) {
            cin >> a[i];
        }
        while(q--){
            int x; cin >> x;
        }
    }
}