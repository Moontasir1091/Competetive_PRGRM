#include <bits/stdc++.h>
#define ll long long int
using namespace std;

int main() {
    int t; cin >> t;
    while(t--) {
        ll n, x; 
        cin >> n >> x;
        
        vector<ll> v(n);
        for(ll i = 0; i < n; ++i) {
            cin >> v[i];
        }
        
        sort(v.begin(), v.end());
       
int hight = v[0];
for(ll i = 0 ; i<n;++i){
    while(hight < x){
        
    }
}


    }
    return 0;
}
